spring.cloud.config.name=gateway-service
spring.profiles.active=development

# Config-First Approach
spring.cloud.config.uri=http://localhost:8888

# Discovery-First Approach
#spring.cloud.config.discovery.enabled=true
#spring.cloud.config.discovery.service-id=configuration-server

# Authentication required for fetching configuration from Git
spring.cloud.config.username=root
spring.cloud.config.password=root

# Eureka URL - If Discovery-First Approach then must specify in bootstrap.properties only
#eureka.client.serviceUrl.defaultZone=http://localhost:8889/eureka/